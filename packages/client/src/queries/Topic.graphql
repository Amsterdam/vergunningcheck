query topicDetail($slug: String!) {
  topic(slug: $slug) {
    hasIMTR
    intro
    name
    slug
    outcomes {
      floLegalOutcomes
      text
    }
    text {
      heading
      locationIntro
    }
  }
}
