query topicDetail($slug: String!) {
  topic(slug: $slug) {
    hasIMTR
    checkerJSON
    intro
    name
    outcomes {
      results
      text
    }
    slug
    text {
      heading
      locationIntro
    }
  }
}
