import styled from "styled-components";

// This component is only to use with `scrollToRef`
// The styling is used to make this component invisible to the user
export default styled.a`
  display: absolute;
  text-indent: -999em;
`;
