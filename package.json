{
  "name": "vergunningcheck",
  "private": true,
  "repository": {
    "url": "https://github.com/Amsterdam/vergunningcheck"
  },
  "engines": {
    "node": "13.1"
  },
  "scripts": {
    "start": "lerna run --parallel --no-bail start",
    "start:prod": "lerna run --parallel start:prod",
    "test": "CI=true lerna run test",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "prepare": "git checkout develop && STTR_ENV=production lerna run sttr && echo 'Please commit changes and run \"npm run release\".'",
    "release": "git checkout release && git pull && git merge origin develop && git push",
    "publish": "git checkout release && lerna-changelog && lerna publish",
    "post-publish": "git checkout develop && git merge release && git push"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lerna": "^3.22.0",
    "lerna-changelog": "^1.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream precommit"
    }
  }
}
